---
title: useQuery
description: The hooks provided by `@trpc/react-query` are a thin wrapper around @tanstack/react-query.
---

# useQuery

Getting some nice formatted code here: `const x = 5;{:ts}`

And here:

```ts showLineNumbers title="post/page.tsx"
import { trpc } from '~/utils/trpc';

const post = await trpc.post.list.useQuery();
```

Even got highlighting:

```ts showLineNumbers title="post/page.tsx" /publicProcedure/#r {8-10}
import { z } from 'zod';

import { publicProcedure, router } from './trpc';

const appRouter = router({
  // Create public procedure at path 'hello'
  hello: publicProcedure.query(() => {
    return {
      greeting: 'hello world',
    };
  }),
});
```

Just gotta fix the twoslash

```ts twoslash
const x = 5;
//    ^?
```
